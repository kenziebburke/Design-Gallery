const Error = () => {
    return(
        <section className="error">
            <div className="wrapper flexContainer">
            <p>Whoops! looks like you have gotten lost along the way, navigate home through the logo in the top left corner!</p>
            </div>
        </section>
    )
}

export default Error